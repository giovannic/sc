# Database Configuration (for local PostgreSQL)
# To use a different database, either:
# 1. Modify these values and run: createdb -U POSTGRES_USER sc_server
# 2. Set DATABASE_URL directly below and ignore these variables

POSTGRES_DB=sc_server
POSTGRES_USER=sc_user
POSTGRES_PASSWORD=sc_password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Server Configuration
NODE_ENV=development
PORT=3000

# Optional: Override database URL directly
# If set, the variables above are ignored
# DATABASE_URL=postgresql://sc_user:sc_password@localhost:5432/sc_server
